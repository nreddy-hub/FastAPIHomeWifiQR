# SQL Server Service
apiVersion: v1
kind: Service
metadata:
  name: sqlserver-service
  namespace: wifi-qr
  labels:
    app: sqlserver
spec:
  type: ClusterIP
  ports:
  - port: 1433
    targetPort: 1433
    protocol: TCP
    name: sql
  selector:
    app: sqlserver
